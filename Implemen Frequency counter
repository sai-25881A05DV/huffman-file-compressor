#include <iostream>
#include <map>
using namespace std;

int main() {

    string text = "hello";

    map<char,int> freq;

    for(char ch : text) {
        freq[ch]++;
    }

    for(auto x : freq) {
        cout << x.first << " : " << x.second << endl;
    }

    return 0;
}
